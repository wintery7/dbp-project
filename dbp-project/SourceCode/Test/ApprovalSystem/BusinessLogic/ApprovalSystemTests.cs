using DeepCheeseBacon.SourceCode.ApprovalSystem;
using System;
using System.Collections.Generic;
using System.Text;

using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace YourUnitTestProjectNamespace
{
    [TestClass]
    public class ApprovalServiceTests
    {
        //[TestMethod]
        /*public void TestSaveApproval()
        {
            // Arrange
            ApprovalService approvalService = new ApprovalService();
            Approval approval = new Approval();

            // Act
            int savedApprovalId = approvalService.SaveApproval(approval);

            // Assert
            Assert.IsTrue(savedApprovalId > 0);
        }

        [TestMethod]
        public void TestApproveRequest()
        {
            // Arrange
            ApprovalService approvalService = new ApprovalService();
            Approval approval = new Approval();
            ApprovalApproveRequest request = new ApprovalApproveRequest { ApprovalId = approval.Id };

            // Act
            approvalService.ApproveRequest(request);
            approval.Status = Approval.ApprovalStatus.Approved;

            // Assert
            Assert.AreEqual(Approval.ApprovalStatus.Approved, approval.Status);
        }

        [TestMethod]
        public void TestRejectRequest()
        {
            // Arrange
            ApprovalService approvalService = new ApprovalService();
            Approval approval = new Approval();
            ApprovalRejectRequest request = new ApprovalRejectRequest { ApprovalId = approval.Id, Memo = "Rejected due to some reason" };

            // Act
            approvalService.RejectRequest(request);
            approval.Status = Approval.ApprovalStatus.Rejected;

            // Assert
            Assert.AreEqual(Approval.ApprovalStatus.Rejected, approval.Status);
        }*/

     }
}
